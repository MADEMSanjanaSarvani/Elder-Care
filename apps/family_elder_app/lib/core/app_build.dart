/// A single, human-readable build stamp shown on the login and settings
/// screens. Bump this on every build you hand to a tester so it is instantly
/// obvious whether the installed APK is the newest one — no more "did the
/// install actually update?" ambiguity.
///
/// Keep it in sync with pubspec `version:`.
const String kAppBuildLabel = 'v0.7.0 · build 45 · 27 Jul';
