# Package convention (unlike the two apps, which commit pubspec.lock for
# reproducible builds): a library's lockfile is regenerated by whatever
# consumes it, so it's not committed here.
.dart_tool/
.packages
pubspec.lock
build/
